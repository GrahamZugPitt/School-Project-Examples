#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>
#include <time.h>

int main(){

	printf("sizeof(unsigned int) = %d\n", (int)sizeof(unsigned int));
	printf("sizeof(char) = %d\n", (int)sizeof(char));
	printf("sizeof(short) = %d\n", (int)sizeof(short));
	short gay = 3.5;
	printf(%s, gay);
return 0;
}
